apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "_chart.fullname" . }}-test-connection"
  labels:
    {{- include "_chart.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "_chart.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
