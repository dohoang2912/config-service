apiVersion: v1
kind: Service
metadata:
  name: {{ include "_chart.fullname" . }}
  labels:
    {{- include "_chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "_chart.selectorLabels" . | nindent 4 }}
